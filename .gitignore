venv/
__pycache__/
*.py[cod]
.DS_Store
.env
results.json
stress_results.json
custom_evaluator_1_results.json
data/catalog.jsonl
data/catalog.jsonl.gz
data/releases/
# Organizer-only code, private evaluation data, manifests, and build reports.
organizer/
secure/
# Internal provenance and release-audit working documents.
docs/audits/
docs/data_selection_audit.md
docs/participant_release_checklist.md
# Organizer-only pipeline tests.
tests/test_5core_builder.py
tests/test_organizer_pipeline.py
*.log
# Local model weights (e.g. fastembed ONNX cache) — never commit; large + reproducible.
models/
# Regenerable robust-eval outputs (see scripts/evaluate_robust.py).
benchmark-results/
# Regenerable robust-eval CASES (contain sample_ids + ground-truth ASINs —
# leakage-shaped; rebuild with `python -m scripts.gen_robust_cases`).
data/robust/
# Stray session/telemetry artifact.
:memory:.ses
